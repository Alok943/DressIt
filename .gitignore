# secrets
.env
*.env.local

# local project context for Claude (not shared)
CLAUDE.md
# personal Claude permission allowlist (machine-local); launch.json is shared
.claude/settings.local.json

# python
__pycache__/
*.pyc
.venv/

# node
node_modules/
dist/

# large raw data (regenerate via pull scripts); keep normalized + catalog small
data/raw_*.json

# backend runtime logs
backend/reactions.jsonl
reactions.jsonl

# merge_catalog.py / fix_categories.py local safety backups (regenerated each run)
frontend/public/catalog.bak.json
frontend/public/catalog*.bak.json
